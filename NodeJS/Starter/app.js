//! to enter REPL -> open cmd and type "node"
//? to exit -< press ctrl + c or .exit
//? this is a shell or interface using which we can write js code.

//! to execute js code using node -< "node filename.js" (ext is not mandatory)

console.log("hello");
//? in order to execute any file, the correct path should be opened in the terminal
// console.log(globalThis);
// console.log(global);

// console.log(window);

//? in case of nodejs the name of global object is "global"
